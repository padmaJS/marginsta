<%= turbo_stream.update "follow_button_#{@user.id}" do %>
  <%= link_to "Follow", follow_user_path(@user.user_name), class: "btn btn-primary btn-sm px-4 fw-bold", data: { turbo_frame: "follow_button_#{@user.id}", turbo_method: :post } %>
<% end %>