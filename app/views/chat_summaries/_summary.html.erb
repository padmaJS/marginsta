<div class="chat-summary alert alert-info mt-3" id="<%= dom_id(summary) %>">
  <div class="d-flex justify-content-between align-items-start mb-2">
    <strong>🤖 AI Summary</strong>
    <small class="text-muted"><%= time_ago_in_words(summary.created_at) %> ago</small>
  </div>
  <p class="mb-1"><%= summary.content %></p>
  <small class="text-muted">Based on <%= summary.message_count %> recent messages</small>
</div>
