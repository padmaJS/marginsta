<nav class="navbar navbar-dark bg-dark border-bottom shadow-sm">
  <div class="container-fluid justify-content-between px-4">
    <div class="d-flex align-items-center gap-3">
      <%= link_to root_path, class: "navbar-brand d-flex align-items-center gap-2" do %>
        <i class="bi bi-instagram" style="font-size: 2rem;"></i>
        <span class="fw-bold">Marginsta</span>
      <% end %>
    </div>
    <div class="d-flex align-items-center gap-4 text-light">
      <% if user_signed_in? %>
        <%= link_to chats_inbox_path, class: "nav-link px-2" do %>
          <i class="bi bi-chat-dots"></i>
          <span class="d-none d-md-inline">Chats</span>
        <% end %>
        <%= link_to new_post_path, class: "nav-link px-2" do %>
          <i class="bi bi-plus-square"></i>
          <span class="d-none d-md-inline">New Post</span>
        <% end %>
        <%= link_to destroy_user_session_path, data: {turbo_method: :delete}, class: "nav-link px-2" do %>
          <i class="bi bi-box-arrow-right"></i>
          <span class="d-none d-md-inline">Logout</span>
        <% end %>
      <% else %>
        <%= link_to new_user_registration_path, class: "nav-link px-2" do %>
          <i class="bi bi-person-plus"></i>
          <span class="d-none d-md-inline">Register</span>
        <% end %>
        <%= link_to new_user_session_path, class: "nav-link px-2" do %>
          <i class="bi bi-box-arrow-in-right"></i>
          <span class="d-none d-md-inline">Login</span>
        <% end %>
      <% end %>
    </div>
  </div>
</nav>